{
  "name": "common",
  "version": "1.0.2",
  "main": ".build/index.js",
  "types": "./build/index.d.ts",
  "license": "MIT",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "npm version patch && git add . && git commit -m \"build: publishing library\" && npm run build && npm publish"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.7.4"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/@matyah"
  }
}
