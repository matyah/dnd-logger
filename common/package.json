{
  "name": "common",
  "version": "1.0.1",
  "main": ".build/index.js",
  "types": "./build/index.d.ts",
  "license": "MIT",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"build: publishing library\" && npm version patch && npm run build && npm run publish"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.7.4"
  }
}
